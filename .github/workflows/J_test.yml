name: Automation
on:
  workflow_dispatch:
jobs:
  run-scripts:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install -D playwright
          npx playwright install chromium
      - name: Run automation scripts
        env:
          J1_TEST_JS: ${{ secrets.J1_TEST_JS }}
          J1_KYUEN_JS: ${{ secrets.J1_KYUEN_JS }}
        run: |
          # J1_TEST_JS を実行
          echo "Running J1_TEST_JS..."
          echo "$J1_TEST_JS" > j1_test.js
          node j1_test.js
          
          # 5回のループ
          for i in {1..5}; do
            echo "=== Cycle $i/5 ==="
            
            # 2〜3分のランダム待機
            WAIT_TIME=$((120 + RANDOM % 61))
            echo "Waiting ${WAIT_TIME} seconds..."
            sleep $WAIT_TIME
            
            # J1_KYUEN_JS を実行
            echo "Running J1_KYUEN_JS (1st time in cycle $i)..."
            echo "$J1_KYUEN_JS" > j1_kyuen.js
            node j1_kyuen.js
            
            # 2〜3分のランダム待機
            WAIT_TIME=$((120 + RANDOM % 61))
            echo "Waiting ${WAIT_TIME} seconds..."
            sleep $WAIT_TIME
            
            # J1_KYUEN_JS を再度実行
            echo "Running J1_KYUEN_JS (2nd time in cycle $i)..."
            node j1_kyuen.js
          done
          
          echo "All cycles completed!"
      - name: Cleanup
        if: always()
        run: |
          rm -f j1_test.js j1_kyuen.js
