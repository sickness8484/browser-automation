name: Automation
on:
  workflow_dispatch:
jobs:
  run-scripts:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install -D playwright
          npx playwright install chromium
      - name: Run automation scripts
        env:
          J1_TEST_JS: ${{ secrets.J1_TEST_JS }}
          J1_KYUEN_JS: ${{ secrets.J1_KYUEN_JS }}
        run: |
          echo "$J1_TEST_JS" > j1_test.js
          echo "$J1_KYUEN_JS" > j1_kyuen.js
          
          # ── 事前実行 ─────────────────────────
          echo "Running J1_TEST_JS..."
          node j1_test.js
          
          # ── 10回のループ ─────────────────────────
          for i in {1..10}; do
            echo "=== Cycle $i/10 ==="
            
            WAIT_TIME=$((60 + RANDOM % 70))
            echo "Waiting ${WAIT_TIME} seconds..."
            sleep $WAIT_TIME
            
            echo "Running J1_KYUEN_JS (1/2 in cycle $i)..."
            node j1_kyuen.js
            WAIT_TIME=$((60 + RANDOM % 31))
            echo "Waiting ${WAIT_TIME} seconds..."
            sleep $WAIT_TIME
            echo "Running J1_KYUEN_JS (2/2 in cycle $i)..."
            node j1_kyuen.js
          done
          
          echo "All done!"
      - name: Cleanup
        if: always()
        run: |
          rm -f j1_test.js j1_kyuen.js
