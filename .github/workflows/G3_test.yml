name: Automation
on:
  workflow_dispatch:
jobs:
  run-scripts:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install -D playwright
          npx playwright install --with-deps chromium
      - name: Run automation scripts
        env:
          G3_TEST_JS: ${{ secrets.G3_TEST_JS }}
          G3_KYUEN_JS: ${{ secrets.G3_KYUEN_JS }}
        run: |
          set +e
          run_node_safe() {
            local label="$1"
            local file="$2"
            echo "Running ${label}..."
            node "$file"
            local code=$?
            if [ "$code" -ne 0 ]; then
              echo "::warning title=${label} failed::exit code ${code}"
            fi
            return 0
          }
          
          printf '%s\n' "$G3_TEST_JS" > g3_test.js
          printf '%s\n' "$G3_KYUEN_JS" > g3_kyuen.js
          
          # ── 事前実行 ─────────────────────────
          run_node_safe G3_TEST_JS g3_test.js
          
          # ── 繰り返し実行: 7回 ─────────────────────────
          for i in {1..7}; do
            echo "=== Repeat $i/7 ==="
            
            WAIT_TIME=$((90 + RANDOM % 30))
            echo "Waiting ${WAIT_TIME} seconds..."
            sleep $WAIT_TIME
            
            run_node_safe G3_KYUEN_JS g3_kyuen.js
          done
          
          echo "All done!"
      - name: Cleanup
        if: always()
        run: |
          rm -f g3_test.js g3_kyuen.js
